<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".StoryTrimApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StoryTrim.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:label="登录" />

        <activity
            android:name=".ui.login.RegisterActivity"
            android:exported="false"
            android:label="注册" />

        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false"
            android:label="首页" />

        <activity
            android:name=".ui.points.PointsActivity"
            android:exported="false"
            android:label="积分" />

        <activity
            android:name=".ui.tasks.TaskCenterActivity"
            android:exported="false"
            android:label="任务中心" />

        <activity
            android:name=".ui.shelf.ShelfActivity"
            android:exported="false"
            android:label="书架" />

        <activity
            android:name=".ui.reader.ReaderActivity"
            android:exported="false"
            android:label="阅读" />

        <activity
            android:name=".ui.debug.DebugSqlActivity"
            android:exported="false"
            android:label="SQL 调试" />

        <service
            android:name=".ui.reader.tts.TtsForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <service
            android:name=".ui.reader.tts.FloatingLyricsService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

</manifest>
